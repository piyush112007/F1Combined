# Git Policy

- Do NOT automatically run `git push` or push changes to remote origins.
- Only stage and commit files locally when requested, but leave pushing to the remote repository for when the user explicitly asks for a push.
